---
# Main entrypoint for role_host_prep

- name: Enforce strict permissions on host directories
  ansible.builtin.import_tasks: directories.yml

- name: Initialize secrets and TLS certificates
  ansible.builtin.import_tasks: secrets_and_tls.yml

- name: Provision network bridge and persistent named volumes
  ansible.builtin.import_tasks: network_and_volumes.yml
