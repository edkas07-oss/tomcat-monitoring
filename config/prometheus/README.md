# Prometheus Configuration Contract

Directory ini dicadangkan untuk scrape configuration, alert rules, dan
configuration non-secret Prometheus. Network endpoint, Telegraf scrape
interface, storage, retention, dan deployment target belum diputuskan.

Jangan menambahkan configuration executable sebelum nilai contract yang
diperlukan telah disetujui dan memiliki validator lokal.
